# 2026-07-16

## Forged saves at ANY map — the console authors them itself (Twilight's brief, same day)

Twilight briefed the full thing mid-session: the MCP server must generate **proper save files at any
map state** — tileset/blockset right, sprites right, "the pointers right, lots of little calculations
on the side," world flags generally right — so a Claude session can just ask for a state and test.
Built, and the design dodges every fragility she warned about: **we don't construct the state, the
game does.** `scripts/emu/forge_map_save.py` boots BaseSAV, rewrites Red's front-door warp
destination in live WRAM, steps in, lets the game's own map loader author everything, then replicates
`SaveSAVtoSRAM` off the settled console into a copy of BaseSAV. Full write-up (layout table, the two
routes, the self-checks): [`reference/forged-saves.md`](../../reference/forged-saves.md).

- **A real Gen 1 mechanism found on the way:** `LoadTilesetHeader` skips `LoadDestinationWarpPosition`
  when source and destination share a tileset — warp Pallet→Route 22 (both tileset 0) and the player
  keeps the old coords + view pointer (the machinery behind the classic same-tileset warp glitches).
  The generator therefore reaches outdoor targets from INSIDE Red's house (tileset 1), leaving via the
  exit mat (which fires on the standing-on-warp collision path, pressing DOWN — not on step-on).
- **The 8 warpless routes** (3/9/13/14/17/19/21/24) go by **edge hop**: neighbour's save, offline
  relocation to a collision-checked column on the connection edge (our own `.blk`+`.bst`+`passable`
  data, bottom-left "feet" tile — same sampling as `MapSim::tilePassable`), one step across, each
  attempt its own single-shot process. Route 3 verified via Pewter's east edge.
- **`relocate()`** (in `forge_save.py`, shared): coords move WITH the side-calculations — block
  coords = `coord & 1`, view pointer = `MapEngine::viewPointer` verbatim (re-confirmed against
  BaseSAV), bounds-checked. `forge()` now REFUSES a cross-map id (the chimera) and routes coords
  through it.
- **Console-verified across map classes:** Viridian, Pewter, Lavender, Rock Tunnel, Viridian Forest,
  Route 22, Route 3 — all generated, booted, walked, screenshot-checked. Self-checks gate every dump
  (map id, dims vs maps.json, view-pointer formula, crash-after-load guard).
- **`tst_flag_scenarios`: 5/5 in 2.7 s** (was 542 s of hangs). `route22-control` flipped hang →
  **healthy** on the real base. And a finding: the **suspected Route 22 rival conflict did NOT
  reproduce** — the up-walk from (25,8) is terrain-blocked (screenshot) and with both battles "done"
  the rival objects hide; verdict recorded as **inconclusive, unconfirmed** — reproducing it needs the
  map-SCRIPT state (the scripts import Twilight noted isn't done) + a walkable approach. Exactly the
  suspected-vs-confirmed distinction the conflicting-flags system draws.
- MCP: new `emu_make_map_save`; `emu_boot(map_id=…)`/`emu_forge_save(map_id=…)` now ride the
  console-authored bases — the **total custom state resume** in one call. Scenario runner uses the
  same cache (missing bases generated in fresh subprocesses).

## The PyBoy hang SOLVED, the dev MCP server built, the debug system tightened

Twilight opened with the unexplained disaster: PyBoy runs "spawning tons of processes and hanging,"
the previous session unable to get a clean automated save-file test. Diagnosed to the root, fixed at
every layer, and the whole dev workflow got the standardized transport she asked for.

### The root cause, finally — THREE stacked problems, all reproduced

1. **A map-id-only forge is a CHIMERA and it hard-wedges the console.** Forging just
   `wCurMap`(+coords) onto another map leaves the save's whole Area block (dims/tileset/scripts/
   sprites) belonging to the *old* map — and the `BIT_NO_PREVIOUS_MAP` linchpin means Continue TRUSTS
   it. The console really enters the map (emulator: `map=0x21, w=10, h=9` — Route 22 wearing Pallet's
   body) and **crashes within ~100 frames**: the runaway CPU executes `STOP`, the clocks halt, the
   frame never completes, and **`pb.tick()` never returns** — a full core, forever (the glitch-music
   bad-bank probe recorded the same console behaviour). Isolated cleanly: `forge`-only and `mash`-only
   both run 6,000 frames in ~0.5 s; `forge+mash` wedges after Continue. **This overturns 2026-07-15's
   "forge-onto-any-map PROVEN"** — `on_overworld()` is true for a moment, then the crash lands; the
   probe had stopped looking one moment too soon. Same-map forges (flags/coords/any Main-Data byte)
   remain fully consistent and safe.
2. **Killing the wedged child leaked its interpreter.** The venv `python.exe` is a *launcher* whose
   child is the real interpreter: a plain kill (QProcess::kill, subprocess.kill) can take the launcher
   and orphan the spinning PyBoy. Kill the **tree** (`taskkill /T /F`), always.
3. **The interactive shell's ~44 s cap made both unrecoverable** (the 2026-07-15 diagnosis, confirmed):
   management commands timing out mid-kill leaked more, starved the CPU, timed out more.

### What was built/fixed

- **`tst_flag_scenarios` wired into CMake and live** (the 2026-07-15 pending block): one scenario per
  process, and a timed-out scenario is now recorded as a **"hang" verdict** (tree-killed) instead of
  failing the run — only the control gate hard-fails. `QTEST_FUNCTION_TIMEOUT=900000` rides in its
  environment (initTestCase runs every scenario; QtTest's default 300 s watchdog aborted it otherwise).
  `flag_scenarios.json` committed (it never was) with the chimera limitation noted on the Route 22
  scenarios. Verified live: control healthy, all-flags-on-pallet healthy, Route 22 scenarios → hang,
  every tree confirmed terminated.
- **`scripts/emu/forge_save.py`** — the standing forge method (map/coords/flag names/raw indices/
  arbitrary pokes + checksum reseal) extracted into ONE importable module + CLI; probes and everything
  below share it.
- **`scripts/emu/drive_session.py`** — interactive PyBoy as an owned child REPL (NDJSON over
  stdin/stdout): boots ONCE (a second boot is refused — fresh process instead), frame-bounded loops,
  EOF ⇒ clean stop. Replaces the abandoned file-IPC `emu_server.py` idea with the right ownership.
- **The dev MCP server** — `scripts/mcp/pse_dev_mcp.py` (+ `setup.ps1`, `run.cmd`, README), Twilight's
  brief: one standardized transport for the WHOLE dev loop. ~40 tools: build (kit dir / test dir,
  clean, configure), ctest / single test exes, the full TCP-harness surface (navigate/get/set/click/
  tap/invoke/list/reload), screenshots returned **inline as images** (app and emulator), PyBoy
  setup/update-check/upgrade, single-shot probe runs, the scenario batch, forge-save, an interactive
  emu session (boot at any same-map state, buttons, ticks, memory read/write, framebuffer shots), and
  a leaked-process sweeper. Everything long is a background JOB with a hard **tree-kill** timeout;
  everything runs hidden; `app_foreground()` is the explicit "bring it to her" moment (per the
  2026-07-12 rule). Exact kit toolchain env baked in (llvm-mingw + Qt 6.11 + CMake on PATH,
  CC/CXX=clang, offscreen+fontdir when headless, crash-fast error mode inherited). Reference:
  [`reference/dev-mcp.md`](../../reference/dev-mcp.md). Self-tested end-to-end twice (second run all
  green, including the wedge being caught + tree-killed in 60 s, session recycle, and a poke/readback).
- **The "distorted screenshots" fixed at the source** (Twilight's live report): they were
  **mid-transition grabs** (the New File modal half-faded over the Map screen). `debugserver.cpp`:
  navigation verbs (`screen`/`party`/`back`/`home`) now reply only after the StackView transitions
  finish (polling `busy` via new `appRoot`/`appBody` objectNames, GUI kept running); `shot` waits out
  any in-flight transition and takes an optional `settle` ms. Verified: clean Map + Trainer Card grabs.
- **Harness trap #1 fixed at the source too:** the `screen` verb now **refuses the duplicate push**
  (`"already": true`; `pokemonDetails` exempt — re-navigating switches the mon), and `title` returns
  the registered screen NAME alongside the human title. The dead-copy stack can't happen again.
- Credits: **MCP Python SDK** + **psutil** added (Tools Used, dev-only footing like PyBoy);
  `credits.md` regenerated.

Green: `tst_qml_screens` 16/16 (QML changed: two objectNames), app kit rebuild clean,
`tst_flag_scenarios` exercising the real ROM with hang verdicts. Notes updated:
`reference/dev-mcp.md` (new), `reference/dev-harness.md` (traps #1/#4 fixed),
`reference/emulator-verification.md` (the CORRECTION section), `CLAUDE.md` table, `_nav.dox`.

**Owed / follow-up:** the **consistent cross-map forge** — writing the target map's whole Area block
(everything the editor's model already knows) so "boot onto ANY map" becomes true; Twilight asked for
total custom state control, and same-map state is fully covered today — the cross-map piece needs its
own briefed phase. Also owed: registering the MCP server in Twilight's Claude config (instructions in
`scripts/mcp/README.md`), and her live pass on the new tooling.
