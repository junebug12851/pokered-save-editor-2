# Version History — June 2026

[← All months (index)](../version.md) · 186 commit(s) this month.

---

### 2026-06-15 -- Pokemart: revert error color to literal "red" (primaryColor is pink; theming coming)

Follow-up to the Pokemart modernization below: that pass had swapped the error color from the literal
`"red"` to `brg.settings.primaryColor`. Twilight pointed out `primaryColor` is **pink**, not red — and a
**theming** pass is planned, so screens shouldn't bake a palette role in as "the red". Reverted all four
spots in `Pokemart.qml` (`moneyColor`, the cart-amount field, and the two summary-box signs) back to the
literal `"red"`. VERSION 0.7.4-alpha → 0.7.5-alpha (PATCH).

### 2026-06-15 -- Modernize the Pokemart screen (clean rebuild + fix the stuck Checkout button)

First modernization pass on the Pokemart/Game-Corner shop screen — one of the oldest, buggiest,
half-finished screens. **Look and behavior are unchanged**; the rebuild is internal only. `Pokemart.qml`
delegate was rebuilt off auto-layouts: the fragile `Rectangle { width: 1; height: 50 }` row frame and its
sibling-anchor chain became a clean centered cart stepper with the name/price/owned-count anchored to its
left and the running cost to its right. Stripped the pre-2026 cruft: dead `getTo()` code, the unused
`curName()`, the never-true `=== "phony"` ternary branches, a duplicated `useSigning` check in `moneyStr`,
the four mode-toggled header `Text`s (now one `headerText()`), the four manually-juggled footer status/error
`Text`s (now one `statusText()` with explicit priority). The error color stays the **literal `"red"`** —
deliberately not `primaryColor` (which is pink, and theming is coming, so screens shouldn't bake in a
palette role for a plain red). The per-delegate `isLast` breathing-room hack became a `ListView.footer`. **Fixed the long-noted stuck-hover
Checkout button** at its root in `FooterButton.qml`: a footer button disabled while hovered kept
`hovered == true` (Qt stops sending hover-leave to a disabled item) and stayed highlighted — `hoverEnabled:
enabled` forces it false the instant it disables. VERSION bumped 0.7.3-alpha → 0.7.4-alpha (PATCH). Green:
`tst_qml_screens` (pokemart loads clean), `tst_market_model`, `tst_bridge`.

### 2026-06-15 -- Stop auto-generating screenshot GIFs (still PNGs only; GIFs added manually)

Per Twilight, the screenshot pipeline no longer auto-generates animated GIFs — those will be added
manually, one at a time. Removed the three frame-sequence capture routines (`tileset_anim`, `typing`,
`tab_cycle`) from `screenshooter.cpp`; the capture scripts (`capture_screenshots.{ps1,sh}`) and CI
(`pages.yml`, `release.yml`) no longer install Pillow or auto-assemble GIFs, and Pages no longer copies
`*.gif`. The tool now produces flat `screens/*.png` + `editor/*.png` only. **`scripts/make_gifs.py`
(the Pillow assembler) is kept** for manual GIF assembly, and **Pillow stays credited** (note reworded
to reflect manual use). Notes updated (`screenshots.md`, `deployment.md`, `CLAUDE.md`). Tooling/CI + a
small Credits-note change — no app-logic or save-format change.

### 2026-06-15 -- Fix README blockquote on Pages: upgrade CI Doxygen to 1.17 (+ single-line blockquote)

The README's intro blockquote rendered wrong as the Doxygen home page: no italics (literal underscores)
and a **leaked literal `</blockquote>`** that the following `---` turned into a stray `<h1>` (also
polluting the nav/auto-TOC). **Root cause: CI used ubuntu's Doxygen 1.9.1, which has these Markdown
bugs; the local toolchain is 1.17.0, which renders fine** — so a local `doxygen` test misleadingly
passed while Pages stayed broken. Fix: install **Doxygen 1.17.0 from the official binary** in CI (both
`pages.yml` and the release `docs` job), replacing apt's 1.9.1, so CI matches local. Also hardened the
source — the intro blockquote is now **single-line with `*...*`** emphasis (renders on 1.9.1 and 1.17
and on GitHub alike). Doxygen-markdown gotcha noted in `reference/documentation.md`.

### 2026-06-15 -- GitHub Pages site (docs + screenshots); release-notes + release-policy standards

`.github/workflows/pages.yml` deploys ONE **GitHub Pages** site on every push to `main` (not a release,
not the git repo): the **Doxygen docs home is the root** (`…github.io/pokered-save-editor-2/`) and the
UI images live at `/screenshots/<name>` (+ a gallery). So the docs are browsable online and the README
can embed always-current images, with **zero repo-size growth** and no third party. The docs main page
is the README (`USE_MDFILE_AS_MAINPAGE`, untouched); **Screenshots** + **GitHub** are added as custom
Doxygen top-nav tabs by generating `DoxygenLayout.xml` (`doxygen -l`), injecting two `<tab type="user">`
entries, and building with that layout. It captures headless (`screenshooter` under xvfb; `screens/*.png`
+ `editor/*.png` + GIFs, **excluding `frames/`**), runs `doxygen`, and deploys via
`configure-pages`/`upload-pages-artifact`/`deploy-pages`. Docs reference screenshots by the absolute
Pages URL (works on Pages, GitHub, and the README alike). The README's own screenshot embedding is left
to Twilight (the Pages URLs are ready to use). (Rejected for images: a
rolling images-only release — violates "releases are software-only"; an orphan branch — bloats the repo
object store; Imgur — purges images / deprecated API / needs a secret. Replaced the interim
`screenshots.yml`.) Also: every
GitHub Release now gets a **well-structured title + description by default** (`release.yml` composes a
downloads table + prerelease/unsigned note, with the auto "What's Changed" appended), and Pillow is
pip-installed for the active `python3` so the GIFs make it into the screenshots zip. Standing policy
recorded in `CLAUDE.md` + `reference/deployment.md`: **never release anything that isn't a software
release / never an images-only release**, and release notes are always well-written. `VERSION`
unchanged (CI/deploy infra).

### 2026-06-15 -- Release pipeline green: first release `v0.7.2-alpha` published

The release pipeline now works end-to-end — **`v0.7.2-alpha` is published** with all six artifacts
(Windows portable zip + Inno installer, Linux AppImage + tar.gz, docs zip, screenshots zip). Linux /
docs / version passed first try; the shakeout was all on Windows + one script path:

- **`windeployqt --compiler-runtime`** is MSVC/g++ only → dropped it; copy libc++/libunwind/libwinpthread
  from the compiler bin by hand.
- **`windeployqt` invoked by full path** (`$QT_ROOT_DIR/bin/windeployqt.exe`) — a stray windeployqt on
  PATH hit "Unable to find the platform plugin" and deployed nothing; plus a manual Qt-DLL/QML/plugin
  fallback + a `Qt6Core.dll` / `platforms/qwindows.dll` assertion.
- **Inno Setup `ISCC.exe` path** used `${Env:ProgramFiles(x86)}` (braces required — `$Env:ProgramFiles(x86)`
  mis-parses); Inno is pre-installed on windows-latest.
- **`capture_screenshots.sh`** finds the `screenshooter` binary under `build/tests/` on Linux (build root
  only on Windows). Screenshots render on CI via xvfb + llvmpipe (MultiEffect included).

Also wired GitHub into the default workflow (`CLAUDE.md`): the remote `tests` CI must be green before FF
`main` (not just local `ctest`), the `release` run is watched after, and the issue tracker + PRs are
checked as part of prepping `main` for shipment (event-based, no scheduled ping) with a "now or later?"
prompt — never auto-acting. `VERSION` unchanged (CI/deploy infra).

### 2026-06-15 -- Add the GitHub Actions release/deployment pipeline

Added `.github/workflows/release.yml` to build and publish release artifacts (the deploy half;
`tests.yml` stays test-only). It triggers on push to `main` and releases only when the tag `v<VERSION>`
doesn't exist yet (i.e. `VERSION` was bumped since the last release; `-alpha`/`-beta`/`-rc` → prerelease),
with a `workflow_dispatch` dry-run mode. Each release attaches: Windows portable `.zip` + Inno Setup
installer `.exe`, Linux `.AppImage` + portable `.tar.gz`, Doxygen docs `.zip`, and a UI
screenshots/GIFs `.zip` (the `screenshooter` under xvfb). Toolchain mirrors `tests.yml` (Qt 6.8.3
install-qt-action; llvm-mingw on Windows, gcc on Linux), built Release. New files
`packaging/windows/installer.iss` + `packaging/linux/PokeredSaveEditor.desktop`; new doc
`notes/reference/deployment.md`; Inno Setup + linuxdeploy credited. CI/deploy infra — `VERSION`
unchanged. Not yet exercised on GitHub (first-run shakeout expected, like `tests.yml`).

### 2026-06-15 -- Screenshot tool: capture at a fixed 750×480 (the UI's small-rectangle size)

The screenshots kept looking "too big" because they were captured at 1130×740 — but that's only the
fresh-profile default; the app is designed for a small rectangle (its own saved window is ~751×480).
The tool now captures at a fixed **750×480** (rounded), overridable with `PSE_SHOT_SIZE="WxH"`, and
resizes **before** `start()` so the QML lays out once at the final size (a late resize had left Home's
greyed Maps/Options tiles unrendered). Shots now match the app's actual window. Tooling only. See
`notes/reference/screenshots.md`.

### 2026-06-15 -- Screenshot tool: real GPU window for effects, overlay/popup centering, hover, CI via xvfb

Follow-up fixes after review. The `offscreen` platform (software AND default-RHI, both tested) drops all
`MultiEffect`/layered content (Credits cards, Home's greyed disabled tiles), which made screens look
washed-out/"wrong size"; only a real GPU window renders them, so the tool now runs on the native platform
and shows the view frameless/`Qt::Tool` off-screen (no desktop flash) — and `capture_screenshots.sh` runs
under `xvfb-run` so CI still renders effects headlessly (offscreen+software is the last-resort fallback).
Fixed the quick-edit popup rendering bottom-right: a plain `QQuickView` leaves `QQuickOverlay` at size 0×0,
so `anchors.centerIn: Overlay.overlay` popups don't center — `fixOverlay()` sizes the overlay to the
window. HiDPI grabs are downsampled to the logical 1130×740. The keyboard hover now hovers a real glyph
pill (so the `TilePreview` tooltip shows). Removed the Pokédex hover shot (not needed). Tooling only.
See `notes/reference/screenshots.md`.

### 2026-06-15 -- Screenshot tool: render via a real GPU window (fixes missing content) + HiDPI/typing/View All

Screenshots were missing `MultiEffect`/layered content (Credits showed only its header+background, Home
was missing its greyed disabled tiles) and looked the wrong size — because the `offscreen` +
`QT_QUICK_BACKEND=software` path silently drops those items. The tool now renders through a **real
GPU-backed window** shown frameless/`Qt::Tool`/off-screen (so it draws exactly like the app's
`QQuickWidget` without flashing on the desktop); the `.ps1` no longer forces offscreen (kept as the CI
fallback). HiDPI grabs are downsampled to the logical **1130×740** (a 150% display was yielding
1695×1110; `QT_SCALE_FACTOR` doesn't override screen DPR). The typing GIF now drives the popup's name
field (found by its "Enter a name" placeholder, not the tileset combo) so the textbox owns the value and
the preview follows. Bag's **View All** is opened by clicking the real footer button (the old `shown`-
property poke matched the wrong item on Bag). Tooling only; no app change. See `notes/reference/screenshots.md`.

### 2026-06-15 -- Fix the screenshot pipeline's GIFs (they weren't animating)

The capture pipeline's GIFs didn't animate. Fixes (tooling only, no app change): the static `name_anim`
sequence is replaced by **`tileset_anim`**, which drives the full keyboard's `TilesetDisplay` `curFrame`
0–7 over an Overworld/outdoor tileset (a plain name preview's frames are byte-identical — only tileset
water/flower tiles move); the **`typing`** sequence now drives the name editor's field text + `str` per
step so the live GB-font preview builds up letter-by-letter (synthesized key events never reached the
modal popup field); `make_gifs.py` playback durations were slowed (tab cycle 260 → 850 ms etc.); and the
tool now wipes its output dir each run so renamed sequences leave no stale files. Re-captured at the new
1130×740 default (the harness already drives that size). See `notes/reference/screenshots.md`.

### 2026-06-15 -- UI polish: rival box height, View All tooltip, default window size

Three small UI/UX tweaks. (1) The **Rival** screen's grey card was 360×300 while the Trainer Card's
was 500×250; the rival card now uses height **250** to match the Trainer Card (width unchanged at 360,
per Twilight: match the height only, rival only). (2) In the Pokémon storage **View All** drawer, the
per-count hover tooltip used the default top-left popup placement and floated away from the number — it
now anchors centered under the hovered count (`x: (parent.width - width) / 2`, `y: parent.height + 4`).
(3) The **default window size** is now **1130×740** instead of 640×480: the first-run fallback in
`MainWindow::loadState()` and the `mainwindow.ui` initial geometry, plus the two test-harness sizes
(`tests/helpers/guiapp.h`, `tests/qml/tst_qml_screens.cpp`) that also drive the screenshooter — was
1024×768. Full `ctest` green (66/66). PATCH bump.

### 2026-06-15 -- Headless UI screenshot + animation-capture pipeline

Added an automated, headless way to capture UI screenshots and animated GIFs of the app — no manual
grabbing, no GPU, no real window, and it never touches save data. A new C++ tool
`projects/tests/tools/screenshooter.cpp` (CMake target `screenshooter`, not a CTest test) boots the
real UI offscreen on the shared GUI harness, loads `BaseSAV.sav`, and grabs every top-level screen,
the Pokémon/Bag "View All" drawers, the three Pokémon-editor tabs, both text-editor modes + the
keyboard tileset, hover states, and animation frame sequences — via `QQuickWindow::grabWindow()`.
`scripts/make_gifs.py` (Pillow) assembles the frame folders into GIFs; `scripts/capture_screenshots.ps1`
/ `.sh` build + run + assemble, writing to `tmp/screenshots/` (already git-ignored). Two offscreen
gotchas handled: `QT_QPA_FONTDIR` is set so UI text renders (not tofu), and `QT_QUICK_BACKEND=software`
makes `grabWindow()` produce real pixels with no GPU (CI-friendly). Wired into the Default Workflow to
run by default after a fast-forward of `main`. Pillow credited (Tools Used). Tooling/test only — no
app-code change, so `VERSION` is unchanged. See `notes/reference/screenshots.md`.

### 2026-06-14 -- README: polish Twilight's amended draft

Polished a rough round of Twilight's edits to the README into clean prose without changing their intent. Tightened the new tagline ("A Pokémon Red & Blue save editor written in C++/Qt/QML") and intro, simplified the reboot blockquote to "rebuilt from scratch in Qt/C++", fixed a status-section typo, and reworked the convictions list -- the new **Beautiful UI/UX** conviction, plus reworded **Graceful failure** and **No longer a web app** entries. Rewrote the **Assisted custom-code execution** feature bullet and the **Synthetic Natural** randomization vision from first-person notes into readme prose. Build/test/contributing/architecture sections unchanged.

### 2026-06-14 -- Add `assets/references/` for reference material

Added `assets/references/` as the official home for reference material used while working on the project -- project-folder references and source clones like the `pret/pokered` disassembly or the GitHub sources credited in `credits.json`. The folder is tracked via a `.gitignore` that ignores its contents (`*` but keeps `!.gitignore`), so the material stays local-only and restorable while the folder itself lives in the repo -- the same "tracked folder, ignored contents" pattern as `assets/staging/`. Nothing here is a build input. (First entry written under the new inline-changelog policy: it ships in the same commit as the change, with no hash marker.)

### 2026-06-14 -- Shorten notes changelog name to `version`
<!-- commit: b0cd23aff2bb8256f181e66290f9cd7c063b939e -->
`b0cd23a` - Twilight

With the changelog now living under `notes/`, the `version-info` name was redundant. Renamed `notes/version-info.md` to `notes/version.md` and the folder likewise, updating every cross-reference and relative link. As a subdirectory, `notes/version/` no longer collides with the root `VERSION` file (the original repo-root `version/` collision is why it was once renamed away). `VERSION` untouched.

### 2026-06-14 -- Move the changelog into `notes/`
<!-- commit: 199eb7e70ecdd52e66934100402536d4186e0419 -->
`199eb7e` - Twilight

Relocated the changelog (`version-notes.md` + `version-notes/`) from the repo root into the `notes/` tree as `version-info`, fixing all cross-links and dropping the now-redundant explicit Doxyfile `INPUT` lines (the recursive `notes/` entry already covers them). Historical mentions of the earlier `version/` to `version-notes/` rename were kept and annotated. `VERSION` unchanged.

### 2026-06-14 -- De-name the notes; neutral, shared-project tone
<!-- commit: 6bc16e5839bd54e13bb348b7ffdb7be8ffc492dc -->
`6bc16e5` - Twilight

Swept the notes for personal/maintainer name references and gendered pronouns, moving to a neutral, professional, shared-project tone.

### 2026-06-13 -- README: UI image + project status
<!-- commit: 6811597641ead181c890df2744d68b82dfc333c8 -->
`6811597` - Twilight

Rearranged the UI/UX progress image and refreshed the project-status text in the README.

### 2026-06-13 -- README polish
<!-- commit: 61a9f4e79994bd2a2430ba297d6f0d24985f9068 -->
`61a9f4e` - Twilight

Fixed a doubled word, tidied grammar, and removed a dead Screenshots table-of-contents link.

### 2026-06-13 -- De-glorify the docs; state the goal; archive the 2020 README
<!-- commit: aacf00df1010f52e55e9a3f31a786ab4422f00d8 -->
`aacf00d` - Twilight

Toned down promotional language across the docs, stated the project's actual goal plainly, and archived the original 2020 README as a historical artifact.

### 2026-06-13 -- Rewrite the README
<!-- commit: 96fbb92d5b94831de3e62848e377bcc0db562ed5 -->
`96fbb92` - Twilight

Full README rewrite covering status, features, build/test instructions, contributing, and the randomization modes.

### 2026-06-13 -- Add a tracked-but-ignored `tmp/` scratch folder
<!-- commit: 286eb47196772847834eb54bdf7ea1f0e96c7d9b -->
`286eb47` - Twilight

Added a `tmp/` scratch folder for build/run artifacts -- tracked as a folder but with its contents gitignored.

### 2026-06-13 -- Rename `assets/icons` staging folder to `assets/staging`
<!-- commit: 6eeba9cda9094cf4cf7c5312d00a1d330257ba6b -->
`6eeba9c` - Twilight

The repo-root icons folder is just a gitignored staging area for importing art (distinct from the app icon tree), so it was renamed `assets/staging` to match its purpose. Notes updated; no build inputs affected.

### 2026-06-13 -- Reorganise save fixtures into `assets/saves/`
<!-- commit: 73577828340260ebc6cbf03a1321d8f89edfacd7 -->
`7357782` - Twilight

Reorganised the test save fixtures under `assets/saves/` (natural-clean, synthetic-clean, synthetic-dirty) and moved the structure template in, adding a human-readable `structure.md` wired into Doxygen. All fixture-relative test paths and READMEs updated; full suite green (66/66).

### 2026-06-13 -- Stop tracking Qt Creator's `CMakeLists.txt.user`
<!-- commit: 8e6598a2f042c752ddd83000f5b6db7941755864 -->
`8e6598a` - Twilight

Stopped tracking Qt Creator's per-user `CMakeLists.txt.user` and gitignored it.

### 2026-06-13 -- Note the Windows VERSION checkout gotcha
<!-- commit: 2652422aab281e563116e1ee1b2abcc9ecf0fcd8 -->
`2652422` - Twilight

Documented the Windows case-insensitivity gotcha where checking out old history collides with the `VERSION` file.

### 2026-06-13 -- Rename to bare `VERSION` + `version-notes/`
<!-- commit: dab0a1d493873d5a8b13fc30641a7b23f7a43f84 -->
`dab0a1d` - Twilight

Renamed the changelog folder `version/` to `version-notes/` to clear the case-insensitive collision that had forced the `.txt` suffix, freeing the bare name `VERSION` for the version file. Updated CMake, the Doxyfile `INPUT`, the `VERSION` header comment, and all cross-links; reconfigure confirmed the version still reads 0.7.0-alpha.

### 2026-06-13 -- Correct the i18n note; defer language switching
<!-- commit: bb56fa7fa8794c2ad1252ef3dc254e1f63e46203 -->
`bb56fa7` - Twilight

Corrected the i18n note -- `lupdate` works fine; the apparent hang was a UAC prompt -- and deferred runtime language switching until a second locale and an Options screen exist.

### 2026-06-13 -- Record the i18n pipeline in the notes
<!-- commit: 28083c50e0d61a1a8ac74c6308d1477ec46591dc -->
`28083c5` - Twilight

Documented the new i18n pipeline across `reference/i18n.md`, the decisions, status, next-steps, and the notes index.

### 2026-06-13 -- Qt Linguist i18n pipeline + wrapped UI strings
<!-- commit: a3753631c7f2e9353fa57fcc6a36ce99b99babca -->
`a375363` - Twilight

Stood up a Qt Linguist translation pipeline: `qt_add_translations` producing `pse_en.ts` to `.qm` at `:/i18n`, a `QTranslator` installed early in boot (honoring the `ui/language` setting, else system, English fallback), ~139 QML display literals wrapped in `qsTr()`, router screen titles via `QT_TRANSLATE_NOOP`, and a few C++ model strings in `tr()`. UI chrome only -- game-data names are out of scope.

### 2026-06-13 -- SemVer version system with a single source of truth
<!-- commit: de30af92fc6f2eeb4b68cfa686331a63918fdb3c -->
`de30af9` - Twilight

Introduced a SemVer version system whose single source of truth is the repo-root `VERSION.txt`. CMake reads it, feeds `project(VERSION)`, and generates `pse_version.h` with the git commit appended as build metadata; boot sets the runtime version from it (no more hardcoded `v1.0.0`), About shows the cleaned SemVer, and a Windows VERSIONINFO resource embeds it in the `.exe`. Starting number 0.7.0-alpha; full suite green (66/66).

### 2026-06-13 -- Credit Docker (Tools Used)
<!-- commit: bd88c0c2c4d6599c5aeea86ed69a82e22b47737a -->
`bd88c0c` - Twilight

Living-credits update -- added Docker (now powering the Linux build/test env) under Tools Used.

### 2026-06-13 -- Linux Docker build/test environment
<!-- commit: a597ad0d69ac26286f70f4cae24593c858530cf4 -->
`a597ad0` - Twilight

Added a containerised Qt 6.11 + clang Linux toolchain under `docker/` that rsyncs the repo into a persistent ext4 volume (ccache-cached) and runs the suite four ways via `dtest.ps1`: standard offscreen, ASan+UBSan (which are broken on the Windows kit), real-X xvfb, and llvm-cov coverage. First run all green -- 66/66 each, ASan clean, 89.73% line coverage.

### 2026-06-13 -- Fix heap-use-after-free in the item market
<!-- commit: 5bcd3e413b32dd020f80d08acf372adeb83a1172 -->
`5bcd3e4` - Twilight

Fixed a heap-use-after-free: `totalWorth()` walked the shared static registry calling `_itemWorth()` on entries whose `Item` could already be freed (box rebuild/teardown). Windows read stale-valid memory; only ASan in CI caught it. Switched to `QPointer` with guards so stale entries return a safe default.

### 2026-06-13 -- Byte-fidelity test of the Pokemon editor
<!-- commit: af4c6d6eabc4bfabd5a1c2627c46aa77075ba060 -->
`af4c6d6` - Twilight

Extended `tst_gui_fidelity` to instantiate the full Pokemon editor over a real party mon and browse it non-destructively -- every tab, dropdown, and field -- then assert the flattened save is byte-identical. Covers 9 tabs, 9 dropdowns, 22 fields.

### 2026-06-13 -- E2E test of PC-box drag persistence
<!-- commit: fd8267215ab6664020f7349ef7a7b7dceac1538f -->
`fd82672` - Twilight

`tst_gui_drag` now also drives the PC-box reorder/delete on a runtime-populated box and verifies the changes survive a save then reopen.

### 2026-06-13 -- Fire shortcut verbs through a shared action map
<!-- commit: b5172ce3fe8ca079457c2109fdcb00b73c259155 -->
`b5172ce` - Twilight

Extracted the shortcut-to-verb wiring into a shared `pse::shortcutActions()` used by both MainWindow and the test; `tst_shortcuts` now asserts every shortcut has an action and actually fires the safe non-dialog verbs.

### 2026-06-13 -- Notes: shortcut/drag tests + otherShortcuts fix
<!-- commit: 4e358536f524709fadb6655ce5d44417518ba488 -->
`4e35853` - Twilight

Notes for the new shortcut and drag E2E tests, plus recording the `otherShortcuts` copy-bug fix.

### 2026-06-13 -- Shortcut-binding guard + items drag/drop E2E
<!-- commit: 7a852ee20cde0d9d32b5bd758f0d2eee820f7f00 -->
`7a852ee` - Twilight

`tst_shortcuts` pins every key sequence and guards against collisions; `tst_gui_drag` drives item reorder/transfer/delete through the live bridge and asserts persistence across a save then reopen.

### 2026-06-13 -- Share the shortcut key map; fix empty otherShortcuts
<!-- commit: 9d30509117355becd7e99093e4d9a63e29f2dbfa -->
`9d30509` - Twilight

Moved the global shortcut key sequences into `boot/shortcutdefs.h` so tests can pin them, and fixed an `auto os = otherShortcuts` copy bug that had left the member empty (the shortcuts only survived via their QObject parent).

### 2026-06-13 -- Drop check-CI-by-default; keep the flake record
<!-- commit: d9b3dbc8f87c62786e872e4a657b29f95c0bf300 -->
`d9b3dbc` - Twilight

Dropped the short-lived "check CI by default" guidance while keeping the record of the Qt-download flake fix.

### 2026-06-13 -- Note the CI Qt-download flake mitigation
<!-- commit: 34a115329a93f12e8b2229a2984ceb1dfb1cbced -->
`34a1153` - Twilight

Recorded the CI Qt-download flake mitigation (and, briefly, a check-CI-by-default note).

### 2026-06-13 -- Cache Qt + pin 6.8.3 to fix flaky CI downloads
<!-- commit: 0fe09134e95862aa8ae35d7261ae89bced7ffadb -->
`0fe0913` - Twilight

The ASan CI job kept failing at Install Qt when `aqtinstall` couldn't reach the mirrors. Enabled the install-qt-action cache and pinned the exact Qt version for a stable cache key so a good download is reused.

### 2026-06-13 -- Notes: synthetic fixtures, badgeCount, per-control roadmap
<!-- commit: ad61b19d3e3049ed31cafca4600035cbe461dbce -->
`ad61b19` - Twilight

Documented the synthetic-fixture work and the badgeCount fix, and laid out the remaining per-control testing roadmap.

### 2026-06-13 -- Notes: badgeCount fix + synthetic fixtures
<!-- commit: 557a9078c74ce88d9582b12a7ae9584056e542ae -->
`557a907` - Twilight

Notes capturing the badgeCount stub fix and the synthetic fixture matrix.

### 2026-06-13 -- Synthetic fixture matrix
<!-- commit: 03e281c42a53818329989d6e40914a85695fc12d -->
`03e281c` - Twilight

Added a generator that builds deterministic edge-case saves (maxed/zeroed/all-badges/midgame) from a fresh new file via the engine into `assets/synthetic/`, with a suite asserting each loads, round-trips byte-stable, and carries its edited values; the fidelity test also browses the maxed save. Surfaced the badgeCount stub.

### 2026-06-13 -- Fix badgeCount() to count set badges
<!-- commit: 3f25da827788cb9b702d5f015f42d7cf9329324d -->
`3f25da8` - Twilight

`PlayerBasics::badgeCount()` had been returning the max unconditionally (a 0-badge save reported 8); it now counts the set badge flags per its contract. Found by the synthetic fixtures.

### 2026-06-13 -- Notes: comprehensive testing pass
<!-- commit: 5e413e640db093dffdd2d5cbc4a938ce83311781 -->
`5e413e6` - Twilight

Notes recording the comprehensive testing pass (fidelity, dirty fixtures, editor verification).

### 2026-06-13 -- Verify the editor with a real selection
<!-- commit: 79af2b23f422e1ff1e08ff4e5799fb9da0548b1a -->
`79af2b2` - Twilight

`tst_qml_screens` now injects a real party mon before `completeCreate` so the Pokemon editor's runtime bindings resolve against real data and it's held to the full zero-warning bar.

### 2026-06-13 -- Notes: corrupt-save crash fix + dirty fixtures
<!-- commit: 19634c99b1339690479451029c0b5aa3529370d1 -->
`19634c9` - Twilight

Notes for the corrupt-save crash fix and the new dirty-fixture suite.

### 2026-06-13 -- Dirty/malformed save fixtures + negative suite
<!-- commit: 3aacb2412256ebef597e8ab52aa0e6f7ee334419 -->
`3aacb24` - Twilight

Added `assets/dirty/` (empty/truncated/oversized/all-zeros/all-FF/garbage) and a negative suite asserting wrong-size files are rejected cleanly, right-size garbage loads without crashing, and bad checksums still load. Caught the corrupt-save crash fixed alongside.

### 2026-06-13 -- Clamp out-of-range current-box index on load
<!-- commit: bbb18d982e66bc758d2742b52ecfbec48cf5f268 -->
`bbb18d9` - Twilight

`Storage::load()` indexed `boxes[]` (0-11) with the raw current-box byte (0-127), and a malformed all-0xFF save crashed via a null deref. It now clamps an out-of-range index to 0 -- a no-op for valid saves (byte fidelity intact) and a graceful default for garbage. Found by the dirty fixtures.

### 2026-06-13 -- Note tst_gui_fidelity in the testing plan
<!-- commit: e8f1129634bfb84afcc46b0db4a499f626622cc6 -->
`e8f1129` - Twilight

Added `tst_gui_fidelity` to the testing plan.

### 2026-06-13 -- Byte-fidelity "browsing changes nothing" GUI test
<!-- commit: 9d9db03803db56c451e93e0e824ae3e6545aa76f -->
`9d9db03` - Twilight

New `tst_gui_fidelity`: baseline a save, navigate every screen, open/close every modal and dropdown, focus/blur every field without typing, then flatten+recalc and assert the bytes are identical (reporting any changed offsets) -- across both progressed and new saves.

### 2026-06-13 -- Notes: GUI suite green; Pokemart fix + harness gotchas
<!-- commit: 1498fd0703347dc48184598b9a78245c897c95fb -->
`1498fd0` - Twilight

Notes recording the GUI suite green on the kit, the Pokemart crash fix, and the test-harness gotchas.

### 2026-06-13 -- Comprehensive GUI suite green on the kit
<!-- commit: 91dabef2799c5a1f0c6222b6f75b23ab2e200449 -->
`91dabef` - Twilight

A real-app headless GUI suite on the guiapp harness -- navigation sweep, save/load-through-screens with independence and byte-stability, and synthesized-keystroke input -- plus the load smoke test, all green (61/61). Includes a batch of harness triage fixes (StackView matching/waits, an allowlisted offscreen-font warning, objectName-located fields).

### 2026-06-13 -- Guard the Pokemart empty-cart crash
<!-- commit: d5950d9ca6055d784b6a7477766ad36c1793c3b8 -->
`d5950d9` - Twilight

The Pokemart footer bindings evaluated during the screen push before the list was built, hitting an empty list and a fatal assert. Fixed by building the list in the constructor (valid from construction, independent of nav-slot order) and returning sane empties from the cart accessors. Found by the navigation test.

### 2026-06-13 -- Clarify in CLAUDE.md that PowerShell can build/test/run/git
<!-- commit: 256cca31a055a6f9bbfdee938f960e108c06f0cc -->
`256cca3` - Twilight

Clarified in CLAUDE.md that the PowerShell terminal has real access to build, test, run, and git on the Windows machine -- it is not limited to the sandbox.

### 2026-06-13 -- Fix the Credits screen failing to open
<!-- commit: b21a55bebeff3d7e7a10186969dac3c0f1dece29 -->
`b21a55b` - Twilight

The Credits/About screen never opened due to two QML bugs: a `contentWidth` property on the Page root collided with a FINAL Control property (renamed to `colWidth`), and a Page `id` of `top` collided with the `top` anchor inside delegates, breaking icons/links (renamed to `root`). Verified on-screen; both added to fix-patterns.

### 2026-06-13 -- Redesign Credits/About + data-driven back end
<!-- commit: f6483915bdca5d2944850a5cfdfa90e100d38894 -->
`f648391` - Twilight

Reworked `credits.json` into an ordered sections shape (adding a credit or section is now a pure JSON edit), replaced the eight hardcoded blocks in `CreditDBEntry::process()` with a single data-driven loop, made `CreditsModel` section-grouped, and redesigned About.qml with translucent section cards over the wallpaper, colourised icons, clickable links, and a version/copyright footer. Full suite green (57/57).

### 2026-06-13 -- Credit ChatGPT for the rival artwork
<!-- commit: 791e06303fc9611a4d0080b4b240e8efd81725ed -->
`791e063` - Twilight

Credited ChatGPT for the rival artwork in both the AI Assistance and Icons sections.

### 2026-06-13 -- Record the rival artwork swap
<!-- commit: a502364e94e15d562daae7a5cc108bb7dd1b7651 -->
`a502364` - Twilight

Logged the rival artwork swap in status.md.

### 2026-06-13 -- Use the new colored rival.png
<!-- commit: 0a2aff1fab9f661afcda39c00c8026fc1f7a58ef -->
`0a2aff1` - Twilight

Imported the new colored `rival.png` into the app asset tree and repointed the Rival screen from the old grayscale art (mirrors the trainer.png swap).

### 2026-06-13 -- Record the Rival screen cleanup
<!-- commit: 29bb7632e601e53d732279bd139ea87b2b814463 -->
`29bb763` - Twilight

Logged the Rival screen cleanup in status.md.

### 2026-06-13 -- Clean up the Rival screen to match the Trainer Card
<!-- commit: 719f8b1afa4a216ffdc04aaba70e009b3c9a97c5 -->
`719f8b1` - Twilight

Replaced the Rival screen's magic-margin positioning with a centred bordered card matching the Trainer Card style (shared height knob, divider under the name), kept the simple name/image/starter stack and identical bindings, and routed access through a null-guarded `rival()` helper.

### 2026-06-13 -- Soften the Unformat warning copy
<!-- commit: e606b67f3667f84d6b8a60b692206bfa3d8037bf -->
`e606b67` - Twilight

Softened the Unformat warning -- the data is effectively gone unless recovered before an editor or the game destroys it.

### 2026-06-13 -- Source-verified box format/recovery findings
<!-- commit: 97573686f537c9478effa6a3738982596180341d -->
`9757368` - Twilight

Recorded source-verified Gen 1 box format/recovery findings cross-checked against the pret/pokered disassembly.

### 2026-06-13 -- Render species markup in View All tooltips
<!-- commit: e7312884671de899c4c0f03565b9d1017fb420f9 -->
`e731288` - Twilight

View All tooltips now render species name markup (e.g. Nidoran with a male sign).

### 2026-06-13 -- Box-format triggers + recovery-opportunity theory
<!-- commit: 03ae2c786b5d665e027a22adc5ca744f48f8ef1d -->
`03ae2c7` - Twilight

Notes on the Gen 1 box-format triggers (box-switch / new-file only) and the recovery-opportunity theory.

### 2026-06-13 -- Correct the Unformat warning (erased on box switch)
<!-- commit: 6f810f3d1486784ad0a101ace20564299677b870 -->
`6f810f3` - Twilight

Corrected the Unformat warning -- the data is erased on an in-game box switch, not on open.

### 2026-06-12 -- Lighten Format copy; stress Unformat danger
<!-- commit: fe58bb2659bc58046a0360e4b11650d142ead11f -->
`fe58bb2` - Twilight

Lightened the Format-boxes copy (it just unlocks) while stressing the real data danger on Unformat.

### 2026-06-12 -- Polish the Format-boxes confirm paragraph
<!-- commit: c154192112313b2e0b7c7d8f109ccb4675a0be3b -->
`c154192` - Twilight

Polished the Format-boxes confirmation copy to reflect that the state depends on how the save was loaded.

### 2026-06-12 -- Reword the Format-boxes confirm for clarity
<!-- commit: bd3ad00239e63937e7b7d26391e505252af1d5dd -->
`bd3ad00` - Twilight

Reworded the Format-boxes confirmation middle paragraph for clarity.

### 2026-06-12 -- Clarify the Boxes Formatted warnings (recovery exception)
<!-- commit: 8dc1ff90539a8ce2ea3d0b5f3aba01073a28ef3b -->
`8dc1ff9` - Twilight

Rewrote the format/unformat confirm copy to match how Storage actually behaves -- within a loaded session unformatting is fully reversible by re-formatting; loss only becomes real once the save is reloaded or the game overwrites the freed space.

### 2026-06-12 -- Storage Tools menu + Boxes Formatted warning
<!-- commit: 5cf12c153ee8b84298175cb978c43650dc6b6936 -->
`5cf12c1` - Twilight

Turned the footer "Boxes Setup" button into a Tools (wrench) menu with a "Boxes Formatted: On/Off" toggle that opens a direction-aware confirm (format = erase+format all but the current box; unformat = soft delete with permanent-loss risk), flipping only the `boxesFormatted` bit. Also made the storage grid render species gender markup generically.

### 2026-06-12 -- Species markup on the storage grid; unbold View All headers
<!-- commit: b2d69a6cd38d2fd13c319ddd66dceb90881b519c -->
`b2d69a6` - Twilight

Applied the Pokedex name-fix on the storage selection grid so the gender markers show as symbols, and removed the bold on the View All box-number column headers.

### 2026-06-12 -- View All sort icons + condensed columns
<!-- commit: 2938f53b44391e006bfe5758f9e167ecd19b0c67 -->
`2938f53` - Twilight

The View All header sort button now shows the current order's icon (using three PNGs, aspect-fit so the non-square art isn't squished), and the table was condensed roughly in half via per-column widths so it fits without sideways scroll in the common case.

### 2026-06-12 -- Polish Pokemon View All
<!-- commit: fe772892dd1bba9591ae1ec04486d800efd86cd0 -->
`fe77289` - Twilight

Polished the Pokemon View All overview -- zebra row/column tints with whole-row hover, a wider species column with the Pokedex name-fix, a header sort button cycling Dex/Alphabetical/Internal, and a tooltip that appears only when something is traded (an all-caught no-nickname cell has none). Test extended accordingly.

### 2026-06-12 -- Pokemon storage View All overview drawer
<!-- commit: 74bb2bf16911967d0aab5e9c114ad75947a4a724 -->
`74bb2bf` - Twilight

Added a Party + non-empty-boxes overview table (rows alphabetised by species), each cell showing the per-box count with a hover tooltip of differing nicknames and a caught/traded split, backed by a new read-only `PokemonOverviewModel` with precomputed tooltips and wired through the bridge.

### 2026-06-12 -- Credit ChatGPT for badge images + leader portraits
<!-- commit: 42ed2cda9fb061c173dcdbe41b29c4c4791544c4 -->
`42ed2cd` - Twilight

Credited ChatGPT for the gym-badge images and gym leader portraits, and marked the trainer-card badge/portrait work approved.

### 2026-06-12 -- Normalise badge icons to 256x256 square canvases
<!-- commit: 64797bcd31507c63d3466af3e86477e42ed51c03 -->
`64797bc` - Twilight

Re-processed the badge icons onto uniform 256x256 centred square canvases (content at ~98%) so earned badges and unearned shadows render at a consistent footprint. No QML change.

### 2026-06-12 -- Recrop gym-leader shadows to square busts
<!-- commit: 2af715911e7e8897a40b9fd58b2714d8bf7fb970 -->
`2af7159` - Twilight

Recropped the tall full-body leader silhouettes into square head-and-shoulders busts so they fill the badge cell uniformly with the near-square badge icons.

### 2026-06-12 -- Aspect-fit + center badge grid images
<!-- commit: efdf3b580fdf4b0be23fe303b3df275fb4b32966 -->
`efdf3b5` - Twilight

Switched the badge-grid delegate from Stretch to PreserveAspectFit (with mipmap and a capped sourceSize) so the non-square art isn't distorted, and re-imported the eight badge icons after size optimisation (~1MB to ~360-430KB each).

### 2026-06-12 -- Badge grid: icons (earned) + leader shadows (unearned)
<!-- commit: cdbda00aa9744a561bb486f85497926e05842e2a -->
`cdbda00` - Twilight

The Trainer Card badge grid now shows the badge icon when earned and the gym leader's shadow when unearned (replacing the old on/off pair and the opacity dimming, since the shadow conveys state). Imported the artwork and swapped the qrc entries.

### 2026-06-11 -- Update trainer-card artwork (revised trainer.png)
<!-- commit: 55f0e3777e598a36ffbfbe22256b1efee3df7867 -->
`55f0e37` - Twilight

Updated the Trainer Card front to the revised `trainer.png` artwork.

### 2026-06-10 -- Swap trainer-card artwork to colored trainer.png
<!-- commit: 579e948077f5409c46e217dddff0833c18a2d819 -->
`579e948` - Twilight

Replaced the grayscale Gen-1 sprite on the Trainer Card front with the new colored two-trainer illustration; layout unchanged.

### 2026-06-10 -- Correct status note (View All is a hand-rolled panel)
<!-- commit: d333449d1f537db26027419996b2baa8c45cc902 -->
`d333449` - Twilight

Corrected a status note to reflect that View All is a hand-rolled slide panel, not a Material Drawer.

### 2026-06-10 -- Block hover pass-through; narrow the View All panel
<!-- commit: 586695d6c6e6f620c30218fb96fd22d27789510a -->
`586695d` - Twilight

Stopped the bag rows' hover delete-chip lighting up through the open View All panel by adding a blocking `HoverHandler` to the panel and scrim, and narrowed the panel slightly.

### 2026-06-10 -- View All panel: drop border, block scroll/click pass-through
<!-- commit: 7a0027a7d93eb7aa8ba80e549e9fa085caead24c -->
`7a0027a` - Twilight

Removed the panel's right-edge divider (it read as a grey border) and stopped leftover wheel/clicks from falling through to the panes behind by adding a full-size `MouseArea` + `WheelHandler` to the panel and scrim.

### 2026-06-10 -- Replace the View All Drawer with a hand-rolled panel
<!-- commit: 54a8468ac18137e9f08cbd8ed21c2e1acde72946 -->
`54a8468` - Twilight

The Material Drawer kept leaving a white strip and frame that padding/inset tweaks couldn't fix, so it was replaced with a plain Rectangle we fully control -- x-animated slide, fading scrim that closes on outside click, flush header, no frame -- keeping the same overview table and behaviour.

### 2026-06-10 -- Try to make the View All drawer full-bleed
<!-- commit: 251a26f8a3af71c755bbc8a581ded0ca1f2f0c76 -->
`251a26f` - Twilight

Attempted to make the Drawer full-bleed by zeroing all insets and elevation and giving it a flat background (later superseded by the hand-rolled panel).

### 2026-06-10 -- Remove the white strip above the drawer header
<!-- commit: 23ff6a21fa6e0e16ad4841921672348ffbcdbdfe -->
`23ff6a2` - Twilight

Set the Drawer content padding to 0 to remove the white strip above the accent header.

### 2026-06-10 -- Narrow the View All drawer
<!-- commit: 9f26d18165bf4d92646e80ab45e66439bc73cb12 -->
`9f26d18` - Twilight

Capped the drawer width so the fillWidth Item column stopped eating the slack.

### 2026-06-10 -- "View All" items overview drawer on the Bag screen
<!-- commit: a71853bd4f95618617b20e05307580994a41c5f4 -->
`a71853b` - Twilight

Added a left-edge slide-in drawer (new leftmost footer button) showing a condensed, alphabetised table of every item the save holds -- Item / Bag / Storage -- backed by a new read-only `ItemOverviewModel` that aggregates both boxes by index (so glitch items still appear) and is wired through the bridge. Test added; full suite green (57/57).

### 2026-06-10 -- Checked items show the delete chip in its lit state
<!-- commit: 9831f664f4fa9f9c86d100dff8bd64646fa6ac97 -->
`9831f66` - Twilight

A checked item row now renders the delete chip in its filled red/white-X "armed" look permanently, driven by a `lit = hovered || checked` condition.

### 2026-06-10 -- Reserve the delete-chip slot to stop row reflow
<!-- commit: 76c5be7c2ff3379ba1b642adcadfb771aba1aaa2 -->
`76c5be7` - Twilight

The delete chip toggled with `visible:`, collapsing its layout slot and reflowing the whole row on hover; it's now permanently in the layout and just fades opacity, so nothing resizes.

### 2026-06-10 -- Delete chip transparent at rest; clear of the scrollbar
<!-- commit: 84c1c68671002f78532ec5924a7c5f5449b2138b -->
`84c1c68` - Twilight

Made the per-row delete chip transparent at rest, and fixed the recurring "button under the scrollbar" problem by reserving the scrollbar lane and letting the item combo shrink so the trailing chip stays clickable. Documented the list/grid variant in ui-patterns.

### 2026-06-10 -- Never lose items on stack overflow; cross-pane owned total
<!-- commit: d4a753ca76c958cc5779d9b5436b3189484d8222 -->
`d4a753c` - Twilight

Item transfers no longer clamp a stack to 99 and drop the excess -- overflow becomes its own second row, or the transfer is refused if there's no room (no silent loss, ever) -- and the item dropdown now shows each item's total owned across both panes. New tests; full suite green (57/57).

### 2026-06-10 -- Auto-stack item transfers + grey out duplicate picks
<!-- commit: d9ad035952842c2fb474a2aaee4223eac02fb360 -->
`d9ad035` - Twilight

Transfers now auto-stack onto an existing matching row (clamped to 99) instead of inserting a duplicate, and the item dropdown greys out names already present in the same pane. Pre-existing duplicate save data is deliberately left untouched. New tests; suite green (57/57).

### 2026-06-10 -- Drag-and-drop reorder + cross-pane transfer on the items list
<!-- commit: 07fbeda35b0e47390808eb7010c07304124aff3a -->
`07fbeda` - Twilight

Brought the storage grid's drag interaction to the Bag/PC items list -- rows are DropAreas, the content floats to an overlay while dragging, drag starts from a left grip handle (controls keep their input), and you can reorder within a list or transfer bag-PC. Removed the bulk-action footer in favour of a per-row delete chip. Regression tests added; suite green (57/57).

### 2026-06-10 -- Make credits screen + dev workflow living-by-default
<!-- commit: 3a098890f7ac266f0de3f25e37c26fbeed7fb1ce -->
`3a09889` - Twilight

Added the "Keep the Credits Screen Living" and "Default Workflow" sections to CLAUDE.md (auto build+launch, affected-then-full tests before main, debug/profile with logs, green-gated auto commit/push/FF-main) and reconciled the older git-workflow wording.

### 2026-06-10 -- Box-header UX pass
<!-- commit: 215684b95bf9ad4dec191ed712075c98b2cc7cf2 -->
`215684b` - Twilight

Box-header UX pass -- removed the dropdown fill circle (the count conveys fullness), made the current-box marker a leading triangle, and turned the "set current box" control into an Active toggle on both panes (hidden on Party), with the toggle colour-parameterised to read on the accent bar.

### 2026-06-10 -- AI Assistance credits section + ChatGPT status icons
<!-- commit: 61ae6ea5bc61afc151351f2683d141648cb4f243 -->
`61ae6ea` - Twilight

Added a new AI Assistance credits section (Claude, ChatGPT) in display order, and credited ChatGPT under Icons for the in-app status-symbol icons.

### 2026-06-10 -- Notes: storage HP bar + status badges
<!-- commit: 66e9f725d917646df67cf70f12b0e78ca3306fa7 -->
`66e9f72` - Twilight

Documented the storage HP-bar thresholds, the status-badge decode and corner-sharing with the checkbox, and the RGBA asset import workflow.

### 2026-06-10 -- Ignore the assets/icons staging-folder contents
<!-- commit: 046cdbe5e2057364278cd1c8737181cc8891702f -->
`046cdbe` - Twilight

Ignored the contents of the `assets/icons` staging folder (keeping the folder via its `.gitignore`).

### 2026-06-10 -- Storage-cell HP bar + status badges
<!-- commit: 3a8849504b5d1190dc7d5de556aa039e58573a6e -->
`3a88495` - Twilight

Each storage mon now shows a narrow colour-coded HP bar (green/amber/red matching GlancePane) between the icon and name, and a status-condition badge in the upper-left that yields the corner to the hover/checked checkbox. Added the model roles and the status icons, decoded from the status byte.

### 2026-06-09 -- Notes: Pokemon storage drag & drop
<!-- commit: 7d9f22d269f6203d8e6e29e288e12c6ca4d06b0b -->
`7d9f22d` - Twilight

Documented the storage drag/drop, delete-button, and scoped-checkbox-persistence conventions, plus the icon.width-int gotcha.

### 2026-06-09 -- Drag & drop storage grid (reorder, transfer, delete)
<!-- commit: 18ee8d34d396b57384f23c8927fe0a80ad6250c0 -->
`18ee8d3` - Twilight

Made the storage grid cells DropAreas with a manually-driven Drag -- dropping reorders within a pane or transfers to the other pane at the drop slot (honouring the checked set as a group, with a dashed insertion caret and a drag threshold so plain clicks still open the editor). Replaced the bulk-action footer with a per-cell delete chip; checkbox selection is scoped to persist only across the editor round-trip.

### 2026-06-09 -- Fix include case for Linux CI
<!-- commit: 696104dac747d2ba2c60dfced2d96d10f950ad37 -->
`696104d` - Twilight

Corrected an include's casing so the case-sensitive Linux CI builds.

### 2026-06-09 -- Stop tracking the root CI helper log/script
<!-- commit: b38052bd4d5c8e51c8f5965f81fa075f92725cf3 -->
`b38052b` - Twilight

Stopped tracking the root CI helper log/script (`_ci.log`, `_runci.bat`) and gitignored the patterns.

### 2026-06-09 -- Begin the Pokemon Selection screen
<!-- commit: 971caef8c943625495c21e74ae3295116201535c -->
`971caef` - Twilight

Began work on the Pokemon Selection screen, with cleanup, fixes, and polish.

### 2026-06-08 -- Capture the comprehensive-testing program (handoff)
<!-- commit: b8bacf31f52b7d281e59f61d3180f3bde250324e -->
`b8bacf3` - Twilight

Captured the app-layer testing push and the full comprehensive-testing program as a resumable handoff in the notes.

### 2026-06-08 -- Guard TypesModel's placeholder-row data()
<!-- commit: 736263b2d66943b79854f9c13d8dd03e1a7713ae -->
`736263b` - Twilight

TypesModel's placeholder row 0 fell through to an `at(-1)` crash for undeclared roles (unreachable from the UI, but a latent crash); it now returns an empty QVariant. The test was extended to drive an undeclared role on every row.

### 2026-06-08 -- Drive PokedexModel's uncovered methods
<!-- commit: 9e52623373f4e6df70c6ed142629242d1e8c1be6 -->
`9e52623` - Twilight

Extended `tst_bridge` to cover the PokedexModel methods the `data()` sweep misses -- all three sort comparators, the pageClosing reset, the dex lookup hit/miss, and the per-entry change signal -- lifting the worst app file off 51%.

### 2026-06-08 -- Exhaustive select-model drive
<!-- commit: 354c2442c2565723f417f9a549a0814d013b2e58 -->
`354c244` - Twilight

A generic driver hits every row x declared role through `data()` across the select models (types/nature/species/status/starters/move/item/map), plus the index guards, headerData, and each model's value-row helper over edge values. Noted (gated) the placeholder-row undeclared-role crash.

### 2026-06-08 -- db to 90.2%; fix xAlign() guard
<!-- commit: 07551fbd94cd9fbf8468ba484956705eb0232a06 -->
`07551fb` - Twilight

Extended the db getter tests to cover connect-render math, DB base accessors, the qmlProtect cascade, and several entry getters, and fixed `MapDBEntryConnect::xAlign()`'s guard (it returned 0 for every real map, making its math dead code). All three library layers now >=90%; suite green (56/56).

### 2026-06-08 -- db entry-getter sweep
<!-- commit: 6693899678ee54e7811546b543c8d7fd4aae29f1 -->
`6693899` - Twilight

Drove every getter over each store with deepLink so resolved-link getters take their non-null path; db overall 75->83%.

### 2026-06-08 -- FontSearch + FontsDB coverage
<!-- commit: 78d769a062708e2c1f9c38ae0c561ae841085305 -->
`78d769a` - Twilight

Exhaustive sweeps of FontSearch's filters and FontsDB's code conversion/expansion edge cases (driven from each glyph's real name so they stay data-independent), pushing both files into high coverage.

### 2026-06-08 -- common + mapsearch to 100%; fix sprite-set sentinel crash
<!-- commit: ac65d4a3af68ef75b04e0ffa4f5aab2fb20466b1 -->
`ac65d4a` - Twilight

Covered the Utility/Random QML helpers (common to 100%) and all ~30 MapSearch predicates (mapsearch to 100%), and fixed a sprite-set sentinel bug where the guards treated index 0 as "none" instead of -1, dereferencing a null on maps with no sprite set. db 71.6->75.2%; suite green (53/53).

### 2026-06-08 -- AreaWarps coverage; correct the isType(Cave) note
<!-- commit: ef858acfa9fa4babb48f1957293f813fff3b93fa -->
`ef858ac` - Twilight

Added AreaWarps setTo/randomize/list coverage and proved that `isType(Cave)`/`isType(Outdoor)` match correctly once deepLink resolves the tileset -- the long-standing "matches 0 maps" note was a misdiagnosis of the deepLink landmine. Reverted a defensive bound per Twilight (Gen 1 wild tables are fixed at 10). savefile 89.4->90.2%.

### 2026-06-08 -- Fix 3 area bugs + tst_area_logic
<!-- commit: 5d5b62f2098609c9b6316c45304a6ce03caa7e24 -->
`5d5b62f` - Twilight

Coverage-testing the area family surfaced three bugs (all approved before fixing): an inverted tileset-load ternary that crashed on null and discarded the real tileset otherwise; a `Random_Pokedex` roll that excluded Bulbasaur (off-by-one on the 0-based dex); and an unbounded `setTo` array write. Added `tst_area_logic` regression-guarding all three. Big coverage gains across the area files; suite green (51/51).

### 2026-06-08 -- SpriteData full coverage; crash = deepLink landmine
<!-- commit: 95307f3fa74f74c7297838bc257f72175e74a8fb -->
`95307f3` - Twilight

Investigated the documented sprite-link crash first and proved (with deepLink called) that all 918 map sprites resolve cleanly -- the crash is purely the tracked deepLink-not-called landmine, not a SpriteData defect. Added a 13-case suite taking `spritedata.cpp` from 46% to 100%. savefile 82.5->86.5%.

### 2026-06-08 -- Fix isMinEvs() (all-zero, not any-zero)
<!-- commit: 3cb14a509afe417840adde789bc84494c743b89d -->
`3cb14a5` - Twilight

`isMinEvs()` returned true if any single stat-exp was 0; "minimum EVs" means all zero. The real impact: StatsTab's "Reset EVs" was greyed out whenever one stat-exp was 0 even on a heavily-EV'd mon. Fixed and regression-guarded.

### 2026-06-08 -- Fix empty-slot bug in isMaxPP/isHealed
<!-- commit: 22e1098c5d7fbabd025d969e5c6d6e720feba01a -->
`22e1098` - Twilight

The empty-slot bug also lived in `isMaxPP()`/`isMaxPpUps()`, which counted an empty move slot as "not maxed" so the heal indicator was wrong for any mon with fewer than four moves; both now skip empty slots. Also tracked the type2 single-write-truth and isMinEvs decisions in next-steps.

### 2026-06-08 -- Fix 3 type/reset bugs in PokemonBox
<!-- commit: 0dafe96d67624e6e01fc5ec2a8e17054883f1062 -->
`0dafe96` - Twilight

Fixed three approved type/reset bugs: `update(resetType=false)` no longer clobbers a dual-type mon's type2 to type1; `isCorrected()` treats a record as dual-type only when type2 truly differs from type1; and `isPokemonReset()` judges a reset against the species' real initial moves and PP-Ups. Regression-guarded.

### 2026-06-08 -- PokemonBox/PokemonMove logic tests
<!-- commit: 586454a58a6930098577cbc5fc7b7c0ecaf64e7e -->
`586454a` - Twilight

New 18-case `tst_pokemonbox` covering the active PC/party-mon logic the other suites miss -- PP-Up clamping, move type/validity, randomize, newPokemon scopes, nature/level clamps, trade-status/OT data, cleanup. savefile 78->82.4%; flagged three latent type/reset quirks (fixed in the following commits).

### 2026-06-08 -- Storage verbs coverage (79->98%)
<!-- commit: 26af3700d2856eb5ccbf6fc9add28101f645a26a -->
`26af370` - Twilight

Covered Storage's box-space/freeSpace/deposit/randomize verbs, taking `storage.cpp` from 79% to 98%.

### 2026-06-08 -- Cap recent files at the exact max (off-by-one)
<!-- commit: 5fb68cc77da2ff1b9d26b353183860632bf801d2 -->
`5fb68cc` - Twilight

Fixed a MAX+1 off-by-one so the recent-files list caps at exactly the limit; regression-guarded.

### 2026-06-08 -- FileManagement recent-files + load-error coverage
<!-- commit: e28796adba84312190e02b169cb8d4491304f74e -->
`e28796a` - Twilight

Covered FileManagement's recent-files handling and load-error reporting (64->77%) and flagged the recent-cap off-by-one fixed alongside.

### 2026-06-08 -- PlayerBasics coverage (67->99%)
<!-- commit: c0f09b359a322b47b7c07e8d0aebfe8d866de918 -->
`c0f09b3` - Twilight

Covered PlayerBasics' OT rewrite, randomizers, and accessors, taking it from 67% to 99%.

### 2026-06-08 -- Item fragment coverage (62->97%)
<!-- commit: dfd02c017bb77653e52cce844ccf1aa59ac588e3 -->
`dfd02c0` - Twilight

Covered the Item fragment's constructors, load, randomize, pricing, and clamping, taking it from 62% to 97%.

### 2026-06-08 -- Record the deepLink landmine + push policy
<!-- commit: 9023cbc564856732516e893511d6abbdc3e2063c -->
`9023cbc` - Twilight

Recorded the latent `MapsDB::deepLink` landmine and the push-every-commit git policy.

### 2026-06-08 -- SaveFileIterator cursor coverage (57->100%)
<!-- commit: 24210ca7b93150156779c2e285e00e152fe8f0c2 -->
`24210ca` - Twilight

Exhaustively covered the SaveFileIterator cursor, taking it from 57% to 100%.

### 2026-06-08 -- WarpData + MapConnData coverage
<!-- commit: 881dfa97451d9a61cce6a2c2adf0920caa769c7f -->
`881dfa9` - Twilight

Covered the WarpData and MapConnData DB-population and resolve paths (warpdata 54->98%, mapconndata 71->100%).

### 2026-06-08 -- SignData coverage (40->100%)
<!-- commit: 6eac22b9c6842016c105b600198c8a033652fb30 -->
`6eac22b` - Twilight

Covered SignData's DB-population and randomize paths, taking `signdata.cpp` from 40% to 100%.

### 2026-06-08 -- Cross-layer app->savefile->disk e2e
<!-- commit: 354ebb2c28987415b60fa7ea43b71817f9ca9d6e -->
`354ebb2` - Twilight

Added `tst_app_e2e`: edits made through the app layer are saved to a real file, reopened in a fresh manager, and verified -- plus a write-and-compare proving the bytes on disk equal the in-memory flatten+recalc buffer exactly. Suite green (41/41).

### 2026-06-08 -- Note engine-providers + QML-behavioural tiers complete
<!-- commit: 7157b5fce827dd73e45033b210b386bf92ddb929 -->
`7157b5f` - Twilight

Noted the engine-providers and QML-behavioural test tiers as complete.

### 2026-06-08 -- QML behavioural test of the brg.* chain
<!-- commit: f6e37cdf724e02c3809ccabf1547da0c91528ca6 -->
`f6e37cd` - Twilight

Added `tst_qml_brg`, a Qt Quick Test harness that boots a real Bridge and drives the C++-QML property chain from QML (money/coins/playtime read+write, model reachability), guarding the session-13 "undefined chain" regression from QML's side. Suite green (40/40).

### 2026-06-08 -- Cover the graphics engine + image providers
<!-- commit: dd36ab90846882e4fff44896738bd6246ff75dd7 -->
`dd36ab9` - Twilight

Added a GUI test compiling the app qrc so the tileset/font PNGs resolve, covering the TilesetEngine builders, TilesetProvider (valid tile + blank fallback), and FontPreviewProvider -- closing the last untested app tier. Suite green (39/39).

### 2026-06-08 -- De-flake tst_market_model
<!-- commit: 60d7c301150435d07f1c2add286a00c63d239958 -->
`60d7c30` - Twilight

The market test segfaulted ~1/3 of runs from creating and destroying a Bridge per test method (the real app never churns Bridges); it now uses one shared Bridge with order-independent assertions. Verified zero crashes in 20 runs.

### 2026-06-08 -- Property-based fuzz of the save round-trip
<!-- commit: b16235c3bed468f3e70f7268f87048880b2a48b3 -->
`b16235c` - Twilight

Added `tst_fuzz` -- over 150 deterministically-seeded random field combinations asserting every edit persists across a real flatten->recalc->reopen cycle, the checksum stays valid, and flatten is idempotent. Suite green (38/38).

### 2026-06-08 -- Benchmark suite + named regression guards
<!-- commit: bff87595001cb9e346f1df774390a49e2dc61033 -->
`bff8759` - Twilight

Added `tst_bench` (benchmarks with hard wall-clock ceilings that fail on a hang) and `tst_regression` (one named guard per real historical bug). Suite green (37/37).

### 2026-06-08 -- Record app coverage noise; trust the library layers
<!-- commit: 1cc4acfd96b11a724742b38881ae5bb2ee89871f -->
`1cc4acf` - Twilight

Recorded that the app coverage percentage is measurement noise, and that the library layers plus functional suites are the trustworthy signal.

### 2026-06-08 -- Cover Game Corner (coins) market accessors
<!-- commit: 71f801cf47742e01b782d2c1a2ade78556920678 -->
`71f801c` - Twilight

Added a coins-currency sweep to the market test, covering the Game Corner money/GcPokemon/Message entry subtypes that money-mode never reached. Suite green (35/35).

### 2026-06-08 -- Cover MoveSelectModel list modes
<!-- commit: 0bea8c642e3dd0a764d4987e409633c747be3021 -->
`0bea8c6` - Twilight

Covered MoveSelectModel's general all-moves list, the species-specific rebuild path, move-to-row lookup, and the null-mon fallback. Suite green (35/35).

### 2026-06-08 -- PSE_SHARED_APPCORE for clean coverage
<!-- commit: 7ef91d1aec002b5ad057ef3949ef4167790a7694 -->
`7ef91d1` - Twilight

Added an off-by-default `PSE_SHARED_APPCORE` option so all test exes share one instrumented appcore and llvm-cov merges cleanly -- revealing true app coverage of 58.2% line (the earlier 0%/43.6% were static-lib artifacts). The shipped build stays static.

### 2026-06-08 -- Confirm app coverage % is a tooling artifact
<!-- commit: 87545922da2a6521ebdba71f8bd947a6ad3550ed -->
`8754592` - Twilight

Confirmed the low app coverage figure was a tooling attribution artifact, not a real gap.

### 2026-06-08 -- Correct app coverage to 43.6%
<!-- commit: f2c8256fc8527535101ab1471d7183ace496d741 -->
`f2c8256` - Twilight

Corrected the app coverage figure to 43.6% using a per-file-max union method.

### 2026-06-08 -- Re-measure coverage + static-link caveat
<!-- commit: 6eba77931dc88d960f48ffe5adc20e22773813b3 -->
`6eba779` - Twilight

Re-measured coverage (savefile 73, db 62) and noted the app static-link measurement caveat.

### 2026-06-08 -- Round-trip wild-encounter tables and loaded sprites
<!-- commit: 0aecfe9f9a8d17fbb7d9e53f3a2c8e431ba562eb -->
`0aecfe9` - Twilight

Round-tripped the AreaPokemon wild-encounter tables (rates + both 10-slot lists, with grass-swap) and AreaLoadedSprites. Suite green (34/34).

### 2026-06-08 -- Cover ItemStorageModel + checked ops
<!-- commit: 46a007269943ac9af44280574042c0ccd9c775f8 -->
`46a0072` - Twilight

Covered ItemStorageModel's `data()`/`setData()` roles, the trailing empty placeholder row, and the checked bulk ops over the BaseSAV bag via a real Bridge. Suite green (33/33).

### 2026-06-08 -- Market buy-checkout + Settings color scheme
<!-- commit: 8430ad53e5987c77bdbb8c35ec52fdcce23c58e8 -->
`8430ad5` - Twilight

Added a buy-mode checkout to the market test (covering the store-item/money subtypes) and a Settings test for colour-scheme derivation and tileset preview-index resolution. Suite green (32/32).

### 2026-06-08 -- Note the case-sensitivity include landmine
<!-- commit: c2caa1555615b6121bdb06e29c8974eaddbafbf5 -->
`c2caa15` - Twilight

Documented the Windows-vs-Linux case-sensitivity include landmine.

### 2026-06-08 -- Fix a case-sensitive include breaking Linux
<!-- commit: 6ade594d941b41f27174db9b7c98fd8ea63fb770 -->
`6ade594` - Twilight

Fixed a wrong-case include (`spriteset.h` -> `spriteSet.h`) that Windows accepted but the case-sensitive Linux CI rejected.

### 2026-06-08 -- Re-measure coverage (db 56->63)
<!-- commit: aa2bde39ce62db5269c9983e6d998f48f718092f -->
`aa2bde3` - Twilight

Re-measured coverage after five db suites (db 56->63).

### 2026-06-08 -- AbstractRandomString guard + source coverage
<!-- commit: 01ec841d976d5e5c6bbc49c1cefeed483ae340a6 -->
`01ec841` - Twilight

Added the canonical negative-index guard to `AbstractRandomString::getStoreAt` (the same crash as the nine DBs fixed earlier) and covered all five random-string sources, including draw-without-replacement. Suite green (32/32).

### 2026-06-08 -- Sweep map sub-entry getters
<!-- commit: 18830b1741da2bd797f4b9e0eac16013f9f81f5a -->
`18830b1` - Twilight

Swept every map's child sub-entry getters (warps/signs/sprites/wild mons/connections) -- the lowest-covered db files -- checking `getParent()` links back. Suite green (31/31).

### 2026-06-08 -- Cover the MapSearch fluent finder
<!-- commit: 74951aedb59734cd60ef79d95c6876e19891ccd6 -->
`74951ae` - Twilight

Covered the MapSearch fluent finder -- baseline, disjoint has/no filter behaviour, index-filter edges, accessor agreement, and `pickRandom` staying within the narrowed set. Suite green (30/30).

### 2026-06-08 -- Cover the GB text codec + FontSearch filters
<!-- commit: 1197aee70011f4784c9529508372408c9c368254 -->
`1197aee` - Twilight

Covered the Game Boy text codec round-trip and the FontSearch fluent filters. Suite green (29/29).

### 2026-06-08 -- Exercise ItemDBEntry + MapDBEntry getters
<!-- commit: 9cddcb5492a3353c8991a3d9e793d0b3c7c3a27d -->
`9cddcb5` - Twilight

Drove the getter APIs of the two richest getter-based db entries (ItemDBEntry and MapDBEntry), with size-At consistency checks; noted that price and encounter-rate can be negative sentinels. Suite green (28/28).

### 2026-06-08 -- Re-measure coverage (all layers up)
<!-- commit: 72182a1c705683a43fe36391c5b520afc3719c81 -->
`72182a1` - Twilight

Full 27-suite instrumented re-measure -- every layer up (common 79, savefile 72, app 56, db 56); db now the lowest and next focus.

### 2026-06-08 -- Cover the Poke-mart ItemMarketModel
<!-- commit: 1f3fc4cd7b9e84cff1ca3aa2feb7b3550ac62ea4 -->
`1f3fc4c` - Twilight

Covered the Pokemart ItemMarketModel across all four buy/sell x money/coins modes, with cart tracking and a sell checkout that raises the player's money. Suite green (27/27).

### 2026-06-08 -- Cover PokemonStorageModel bulk checked ops
<!-- commit: fa260c48fbbfabe31eba9d1ba98074f9a4bdd102 -->
`fa260c4` - Twilight

Covered PokemonStorageModel's bulk checked operations (toggle-all, move, delete, transfer) via `setData` on a real Bridge, with ordering verified by object identity. Suite green (26/26).

### 2026-06-08 -- Cover Random chance/coin helpers + range(float)
<!-- commit: 0d233374e6e6f2ec7c0e8a72bf76fef296e182ee -->
`0d23337` - Twilight

Covered the Random chance/coin helpers' threshold edges (over 400 iterations) and `range(float)` bounds including the zero-span case. Suite green (25/25).

### 2026-06-08 -- Correct getStoreAt guards across 9 sub-DBs
<!-- commit: 8dcac5879fe0ffb2b5a10cb6430fa9ed62154777 -->
`8dcac58` - Twilight

`CreditsDB::getStoreAt` had an inverted guard and eight other DBs missed the negative-index check, so `getStoreAt(-1)` crashed; all nine now use the canonical `(ind<0 || ind>=size)` guard. Found by the new store sweep. Suite green (24/24).

### 2026-06-08 -- Cover area child lists
<!-- commit: 8249e48921c891d27e203105a715b84531586bac -->
`8249e48` - Twilight

Covered the area child lists -- warp/sign new/at/swap/remove with byte round-trips, plus sprite list ops and tileset talk-over-tile access. Suite green (23/23).

### 2026-06-08 -- ItemStorageBox + Item behaviour
<!-- commit: 45a6b84339bd79f098d9b8819e80f990b497954e -->
`45a6b84` - Twilight

Covered Item amount-clamp and DB-resolved pricing, and ItemStorageBox add/move/sort/remove/worth/relocate with post-mutation byte round-trips. Suite green (22/22).

### 2026-06-08 -- Cover TilesetEngine wave/blank image math
<!-- commit: 672e33ce807b5836049c688aa28e96ae8987ec7c -->
`672e33c` - Twilight

Covered the TilesetEngine blank-image and water-wave image math (frame identities, symmetry, rotation wrap) as pure QImage math runnable guiless. Suite green (21/21).

### 2026-06-08 -- Re-measure coverage baseline (app 20->39%)
<!-- commit: 7cbfac2cea9a091edf084f644e8e140ad6587458 -->
`7cbfac2` - Twilight

Full 20-suite re-measure -- app/appcore line coverage nearly doubled on the Bridge suite -- recording the per-module table and the exact llvm-cov method.

### 2026-06-08 -- Cover Day Care, Rival, Hall of Fame
<!-- commit: 176a6ddb94d01372a59595b6a1f1e18f8c58a18e -->
`176a6dd` - Twilight

Covered the Day Care (including the empty-destructor regression path), Rival, and Hall of Fame regions with round-trips. Suite green (20/20).

### 2026-06-08 -- Fix PlayerPokedex::reset() blanking the dex to all-true
<!-- commit: b9970f741c928d9789470df87b58c699e5b6e062 -->
`b9970f7` - Twilight

`reset()` passed the dex count as `memset`'s fill value, marking the entire dex seen+owned instead of clearing it (`randomize()` had masked the bug). The fill value is now 0; found and guarded by the new `tst_pokedex`. Suite green (19/19).

### 2026-06-08 -- Deep-link sweep + PokemonDBEntry characterization
<!-- commit: cf74e18a29f96c11236fb4db392a19a020c3b87f -->
`cf74e18` - Twilight

Per-species verification that every resolved-pointer vector (types, initial moves, TM/HM, learnset, evolutions) lines up with its source and resolves, plus a Bulbasaur characterisation anchor. Confirmed the DB pokedex field is 0-based (the documented `dexInd+1` convention). Suite green (18/18).

### 2026-06-08 -- Round-trip the area state tree + PC storage
<!-- commit: 6cbb1ce0e300aa882c100904d600859300fcf7cf -->
`6cbb1ce` - Twilight

Round-tripped the current-area facets (general/audio/player/map/tileset/warp flags) and the Storage container (curBox, boxesFormatted, all 12 boxes). Suite green (17/17).

### 2026-06-08 -- tests_all aggregate so no suite is left unbuilt
<!-- commit: 111b7e46f1e5895c81cb8a91fb9d717b158fc9c5 -->
`111b7e4` - Twilight

ctest had four tests Not Run because CI built a drifting explicit target list; replaced it with a CMake `tests_all` aggregate depending on every registered test exe, so no suite can be left unbuilt.

### 2026-06-08 -- Record the Bridge suite + two app-layer landmines
<!-- commit: 7440a3800609c348ff9574baac8b0164e76c37e1 -->
`7440a38` - Twilight

Recorded the Bridge test suite and two app-layer landmines (the `loadScreens` static-init requirement and the unchecked `getBoxMon`/`getPartyMon` `.at()` contract).

### 2026-06-08 -- Bridge integration suite for the remaining models
<!-- commit: eb832f908fce304ee561c3eb005eddaed1d127a8 -->
`eb832f9` - Twilight

Added a Bridge integration suite that wires every model the way the app does and sweeps each across rows x roles, plus PC storage box ops, the box selector, and Router navigation -- covering the previously-untested models. Suite green (16/16).

### 2026-06-08 -- Drop qtdeclarative/qtsvg from the aqt modules
<!-- commit: 52399a6775a245272a93d688e6e84c2be0e73749 -->
`52399a6` - Twilight

Dropped `qtdeclarative` and `qtsvg` from the aqt module list (they ship in the Qt 6 base package, not as add-ons), keeping only `qtcharts`.

### 2026-06-08 -- Update coverage baseline (savefile 67%, app 20%)
<!-- commit: 5399a583f0a62527d5d6ebb3f65a1492c5c559ca -->
`5399a58` - Twilight

Updated the coverage baseline (savefile 67%, app 20%).

### 2026-06-08 -- Round-trip remaining world regions
<!-- commit: ddbc54874abfa28e52cac0dcae8ebb7eebfa6f47 -->
`ddbc548` - Twilight

Round-tripped the remaining world regions -- hidden items, missables, general flags, and scripts.

### 2026-06-08 -- Round-trip world regions (events/towns/trades/completed)
<!-- commit: 0142235cbb4befbabce155df2b3365dfb86dc1f2 -->
`0142235` - Twilight

Round-tripped the world regions for events, towns, trades, and completion flags.

### 2026-06-08 -- Cover item-select and map-select models
<!-- commit: 7536595ae8735381857fe75c8bf7f8dabba7c904 -->
`7536595` - Twilight

Covered the item-select and map-select app models.

### 2026-06-08 -- Mark the git remote setup complete
<!-- commit: 90cee6dd3e4beb9c9534f5fc374e6e604fb7686b -->
`90cee6d` - Twilight

Marked the git remote setup complete -- default branch `main`, `master` removed.

### 2026-06-07 -- Add the git workflow standards
<!-- commit: f346525776588fa5870ce01a01a79e835992b76d -->
`f346525` - Twilight

Added the git workflow standards (`main` fast-forward-only, frequent `dev`) and a pointer from the notes.

### 2026-06-07 -- Cover 5 more app-layer models
<!-- commit: c7f47bf7f464a5f179dccc77d682c65571496a87 -->
`c7f47bf` - Twilight

Covered five more app-layer models -- species, move, status, starters, and credits.

### 2026-06-07 -- Test-writing progress
<!-- commit: 32dc20d7df22163b8776d26063906fbbbdc783bd -->
`32dc20d` - Twilight

Continued progress writing tests.

### 2026-06-07 -- More test-writing progress
<!-- commit: fb42d69d09dc711e25b5ab1b0bff37bd7c60fa4f -->
`fb42d69` - Twilight

Further progress writing tests.

### 2026-06-07 -- More test-writing progress
<!-- commit: 3a03bfce3afd695f9022dc69ba52fe212399a4a1 -->
`3a03bfc` - Twilight

More progress writing tests.

### 2026-06-07 -- More test-writing progress
<!-- commit: db86e3863bceec04b8dba221dcf4149b07953b54 -->
`db86e38` - Twilight

More progress writing tests.

### 2026-06-07 -- More tests written
<!-- commit: 4af3e0198e8a88a8bb731cab13ec4cc3d8746030 -->
`4af3e01` - Twilight

More tests written.

### 2026-06-07 -- Further tests written
<!-- commit: 580bf02d2c991abe026c8764462be2e3e6b5bf99 -->
`580bf02` - Twilight

Further tests written.

### 2026-06-07 -- Begin adding testing
<!-- commit: eb9686d31505394346c37fd614598f5f32fe3fdc -->
`eb9686d` - Twilight

Began adding the test scaffolding and the first tests.

### 2026-06-07 -- Disable home-screen icons for incomplete features
<!-- commit: f01ae86da413d116dfa58afa73e588e197ae4ba7 -->
`f01ae86` - Twilight

Disabled the home-screen icons that lead to incomplete features.

### 2026-06-07 -- Fix recent-files bug on moved files + file-open errors
<!-- commit: c07d81b4759b3ff40e71aa4fdfad4fce1ac8b544 -->
`c07d81b` - Twilight

Fixed a recent-files bug on moved files and corrected file-open error handling.

### 2026-06-07 -- Slight QML change for comments
<!-- commit: 852a129268cc9ee6fcf3b5141f08ee90dc1b4058 -->
`852a129` - Twilight

A slight QML change for comments.

### 2026-06-07 -- The documentation commit
<!-- commit: 31a0341f887e24c02a5dd9a262759684780c93e0 -->
`31a0341` - Twilight

The big documentation commit -- used AI to macro- and micro-analyse the project and produce the initial documentation pass, as part of resurfacing the project before a full manual review. This is the oldest entry in the June 7-14 revival batch the changelog had been missing.

### 2026-06-06 -- Restoration verified; project ~85% resurfaced
<!-- commit: 480105ad78ec6eec175aa5cc9bc59759ace4b862 -->
`480105a` - Twilight

Twilight's checkpoint marking the project as fully uncorrupted again after the
data-loss scare, and roughly 85% of the way back to where it stood six years
earlier. The actual code changes are small cleanups riding along with that
milestone: the full-keyboard character picker got tidied up (the search
header, criteria, params and the results grid lost some dead weight, with the
results view trimmed by a large chunk), a settings tweak in the bridge, a small
fix in the main window, and the running notes (status and the fix-patterns
reference) were updated to reflect the new state.

### 2026-06-06 -- More restoration progress: builds and opens again
<!-- commit: 13b6816304f170374a0e2c85bcabd754c5e7e8ba -->
`13b6816` - Twilight

A milestone in the recovery: the app compiles and launches once more. This is a
small, surgical commit fixing a handful of bugs left over from the earlier data
destruction -- one-line corrections in the database boot path, the map-select
model, and the fonts database -- plus notes updates capturing what was wrong and
how it was fixed.

### 2026-06-06 -- Manual restoration after corruption and scattered backups
<!-- commit: 87b9648ea7152eb559312d370a08ff8cdd446c6c -->
`87b9648` - Twilight

A large hand-rebuild pulling the project back together from scattered backups
after a corruption event. Touching 78 files across every layer, it restores
heavily damaged sources -- the Pokemon storage model, the font database and font
search, file management, the Hall of Fame and item-storage fragments, area
sign/sprite/warp parsing, the rival screen, and the main window among them --
and brings the notes system (history, principles, session log, status, fix
patterns) back in line. Net it rewrites thousands of lines to undo the damage
rather than to add new behavior.

### 2026-06-05 -- AI-assisted revival and modernization begins
<!-- commit: c9dc210adc5d8c012f35eba4c2a1fd5cccbe904e -->
`c9dc210` - Twilight

The commit that reopens the project after six years and begins modernizing it
with AI assistance, with the stated goal of finally finishing the one project
Twilight most wanted to complete. It is sweeping -- on the order of 400 files --
and lays the groundwork for everything that follows: a full CMake build setup
across the common/db/savefile/app projects, the entire `notes/` knowledge base
(architecture, history, principles, decisions, Qt 6 migration patterns, UI
patterns, diagnostics, session log), and a broad pass modernizing the C++ and
QML to build under Qt 6. Many UI fragments and models are reworked in the
process (notably NameDisplay, the Pokemon-details tabs, the Pokemart screen, and
the item/move/select models). This is the inflection point between the original
2019-2020 effort and the 2026 revival.
